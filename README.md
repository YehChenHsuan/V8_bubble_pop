# ESL 視訊體感單字泡泡遊戲 (V8_bubble_pop)

專為 **ESL 兒童美語 第 8 冊 Lesson 8 Page 18（Words to Remember - Word List）** 設計的體感體操網頁遊戲。
**單元主題**：`Animal Kingdom A-Z 40種野生動物大百科與專長對決`
透過視訊鏡頭偵測玩家手勢，在空中揮動雙手戳破飄浮的單字泡泡，兼顧肢體律動趣味與字彙記憶！

---

## 遊戲特色

1. **自訂勾選練習單字清單 (NEW ✨)**：
   - 點擊開始選單中的「⚙️ 勾選練習單字」即可開啟自訂單字清單。
   - 支援任意挑選想複習的單字，**最少勾選 6 個單字**。
   - 勾選的單字將作為專屬題庫，於遊戲中出題與排布泡泡。
   - 提供「全選」、「清空」、「隨機 6 個」、「隨機 12 個」快捷按鈕與單字真人試聽功能。
   - 選取設定自動儲存於瀏覽器 `localStorage`，下次進入自動保留。
2. **鏡像視訊即時體感（Mirrored WebCam Tracking）**：
   - 玩家看到的畫面經鏡像翻轉（`transform: scaleX(-1)`），手往右揮動，畫面中的魔法星光游標同步往右，直覺自然。
   - 雙引擎支援：優先啟動 Google MediaPipe Hands 進行 AI 指尖辨識；在離線或無 GPU 環境自動降級至高幀率影格像素動態感應（Motion Detection）。
   - 支援觸控與滑鼠點擊，無鏡頭設備亦可暢快體驗。
3. **Page 18 完整 40 個核心單字庫**：
   - 完整收錄本課單字：`ant` (螞蟻)、`cat` (貓)、`horse` (馬)、`jaguar` (美洲豹)、`monkey` (猴子)、`owl` (貓頭鷹)、`rabbit` (兔子)、`turtle` (烏龜)、`wolf` (狼)、`zebra` (斑馬)、`can` (能夠/會)、`hop` (單腳跳/跳躍)、`fly` (飛行)、`climb` (攀爬)、`no` (不是/不行)、`alligator` (短吻鱷)、`dog` (狗)、`ibis` (䴉/朱䴉)、`kangaroo` (袋鼠)、`newt` (蠑螈)、`pig` (豬)、`squirrel` (松鼠)、`unicorn` (獨角獸)、`fox` (狐狸)、`animal` (動物)、`can't` (不能/不會)、`swim` (游泳)、`run` (奔跑)、`yes` (是/可以)、`bear` (熊)、`eagle` (老鷹)、`iguana` (鬣蜥)、`lion` (獅子)、`ostrich` (鴕鳥)、`quail` (鵪鶉)、`tiger` (老虎)、`vulture` (禿鷹)、`yak` (氂牛)、`favorite` (最喜愛的)、`jump` (跳躍)。
   - 整合教材真人錄音 MP3（英中雙語發音）與高解析 WebP 閃卡圖片。
4. **沉浸式音效與語音**：
   - 中央題目卡出現時立即朗讀單字，亦可點選 🔊 按鈕反覆聆聽英文發音或中文解說。
   - 戳破任何泡泡皆會朗讀該泡泡之單字發音。
   - 答對奏響清脆的大調三和弦琶音與金色星光；答錯扣減愛心並發出警示提示音。
5. **即時動態補換泡泡**：
   - 環繞中央閃卡的 6 顆單字泡泡中，若戳錯泡泡，錯誤泡泡會立即爆破並扣 1 顆心，且自動隨機補充一顆新的備選單字泡泡回到該插槽，供玩家繼續挑戰。
6. **雙挑戰模式與排行榜**：
   - **限時挑戰模式（Timed）**：自訂 30s、60s、90s、120s，在時限內累積最高分。
   - **無盡冒險模式（Endless）**：5 顆愛心血條，答錯扣 1 心，扣完結算。
   - 本地 `localStorage` 前 15 名輝煌排行榜（獨立鍵名 `v8_bubble_pop_scores_*`），結算時可輸入姓名登錄。

---

## 檔案結構

```
V8_bubble_pop/
├── index.html                  # 遊戲主頁與介面結構 (含單字勾選面板)
├── style.css                   # Glassmorphism、擬真彩虹肥皂泡、單字勾選網格樣式
├── sound.js                    # MP3 真人英中發音播放、Web Audio 合成爆破音與背景音樂
├── hand_tracker.js             # 鏡像校正、AI 手勢追蹤、影格差動態感測、防抖演算法
├── game.js                     # 核心狀態機、單字勾選過濾、碰撞判定、補位與排行榜
├── V8_flashcards_images/       # 40 個單字的高畫質閃卡圖片
├── V8_flashcards_audios/       # 40 個單字的真人英文與中文 MP3 音檔
└── README.md                   # 說明文件
```

---

## 操作指南

- **設定練習單字**：在首頁點選「⚙️ 勾選練習單字」，挑選至少 6 個想練習的字彙後點選「儲存並套用」。
- **視訊手勢**：將手舉起伸向螢幕上的單字泡泡，看到星光游標碰觸泡泡即可戳破。
- **滑鼠/觸控**：亦可直接用滑鼠點擊或手指觸碰泡泡作答。
- **重聽發音**：點擊中央閃卡右下角之「🔊 英文」按鈕重聽發音，或點擊「🗣️ 中文」聆聽中文說明。
